import { theme } from '@/styles';

export const DEFAULT_ICON_SIZE: number = 16;
export const DEFAULT_ICON_FILL: string = theme.color.gray950;
export const DEFAULT_ICON_STROKE: string = theme.color.gray950;
