import { FC, ReactElement } from 'react';

import * as styles from './styles.css';

const PlaygroundList: FC = (): ReactElement => {
  return (
    <div className={styles.root}>
      심심할 때 구현하는 웹 컴포넌트를 정렬하려고 합니다. 아직 준비 중입니다.
    </div>
  );
};

export default PlaygroundList;
