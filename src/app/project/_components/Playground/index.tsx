import { FC, ReactElement } from 'react';

import * as styles from './styles.css';

const Playground: FC = (): ReactElement => {
  return <div className={styles.root}>준비중입니다.</div>;
};

export default Playground;
